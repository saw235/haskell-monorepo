{
  "snapshot": "POPULATED_FROM_SETTINGS",
  "name": "Haskell Monorepo Environment",
  "build": {
    "dockerfile": "Dockerfile",
    "context": "."
  },
  "install": "bazel build //...",
  "terminals": [
    {
      "name": "Run Selenium Server",
      "command": "java -jar /opt/selenium-server-standalone.jar",
      "description": "Starts the Selenium WebDriver server for browser automation testing"
    }
  ]
}
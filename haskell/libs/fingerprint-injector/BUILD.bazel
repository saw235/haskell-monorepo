load("@rules_haskell//haskell:defs.bzl", "haskell_library")

haskell_library(
    name = "fingerprint-injector",
    srcs = glob(["src/**/*.hs"]),
    src_strip_prefix = "src",
    visibility = ["//visibility:public"],
    deps = [
        "//haskell/libs/antifingerprinting:antifingerprinting",
        "@stackage//:aeson",
        "//:base",
        "@stackage//:containers",
        "@stackage//:text",
        "@stackage//:webdriver",
    ],
) 